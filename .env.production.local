# 后端接口基础地址（记得去全局常量里确认下前缀，默认 /api）
NEXT_PUBLIC_API_BASE_URL=https://tai.templechann.com

# BFF层接口统一传统后端基础地址（记得去全局常量里确认下前缀，默认 /api），如果需要访问第三方API，在服务层自己提供API全路径创建方法即可
BFF_PUBLIC_API_BASE_URL=https://tai.templechann.com

# 是否跳过数据库初始化
SKIP_DB_INIT=false